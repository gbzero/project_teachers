<h1>Materias</h1>
<% if @courses.count <= 0 %>
  <p class='text-danger'>No hay materias disponibles</p><br>
<% end %>
<% if @courses.count > 0 %>
<table class='table table-striped table-bordered table-hover'>
  <thead>
    <tr>
      <th width='50%'>Nombre</th>
      <th width='19%'>Carrera</th>
      <th width='15%'>Semestre</th>
      <th width='8%'></th>
      <th width='8%'></th>
    </tr>
  </thead>
  <tbody>
    <% current_user.materias.each do |course| %>
      <tr>
        <td> <%= link_to "#{course.name}", course_path(course.id) %> </td>
        <td> <% unless course.major_id.nil? %> <%= course.major.name %> <% end %> </td>
        <td> <%= course.semester %> </td>
        <td> <%= link_to 'Editar', edit_course_path(course) %> </td>
        <td> <%= link_to 'Eliminar', course, method: :delete, data: { confirm: '¿Seguro?' } %> </td>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>
<br>
<button class='btn btn-primary btn-sm' type='button' onclick="window.location.href='<%= new_course_path %>'" > Nuevo </button>
